<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wisatakita.app">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WisataKita"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.WisataKita.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" android:theme="@style/Theme.WisataKita.NoActionBar"/>
        <activity android:name=".RegisterActivity" android:theme="@style/Theme.WisataKita.NoActionBar"/>
        <activity android:name=".ProfileSetupActivity" android:theme="@style/Theme.WisataKita.NoActionBar"/>
        <activity android:name=".MainActivity" android:theme="@style/Theme.WisataKita.App"/>
        <activity android:name=".ListActivity" android:theme="@style/Theme.WisataKita.App"/>
        <activity android:name=".DetailActivity" android:theme="@style/Theme.WisataKita.App"/>
        <activity android:name=".GalleryActivity" android:theme="@style/Theme.WisataKita.App"/>
        <activity android:name=".AlbumDetailActivity" android:theme="@style/Theme.WisataKita.App"/>

        <service android:name=".MusicService" android:exported="false"/>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths"/>
        </provider>
    </application>
</manifest>
